# Release process

Credora V1 uses a GitHub Actions release workflow that re-runs the test suite and clean-checkout self-check before creating `v1.0.0`.

A release is considered published only when GitHub Releases contains the `v1.0.0` release.
